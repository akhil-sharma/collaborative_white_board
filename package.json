{
  "name": "collaborative_drawing_board",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix apps/client\" \"npm run dev --prefix apps/server\"",
    "build": "npm run build --prefix apps/client && npm run build --prefix apps/server",
    "start": "npm run start --prefix apps/server"
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "typescript": "^5.6.2"
  }
}
